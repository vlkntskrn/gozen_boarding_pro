name: gozen_boarding_pro
description: GOZEN BOARDING PRO - Boarding & Watchlist Operations
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI / Helpers
  intl: ^0.19.0

  # Firebase
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0

  # Camera / Barcode-QR Scanner
  mobile_scanner: ^5.2.3

  # File + Share (xlsx / digest export)
  path_provider: ^2.1.4
  share_plus: ^10.1.4

  # Excel (.xlsx)
  excel: ^2.1.0

  # Web download helper (xlsx web export için)
  universal_html: ^2.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
